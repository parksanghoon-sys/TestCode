<UserControl x:Class="wpfMVVM.Popup.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfMVVM.Popup.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Button Content="Show Confirm MessageBox"
                    Command="{Binding ShowConfirmMsgCommand}"/>
            <Button Content="Show Confirm2 MessageBox"
                    Command="{Binding ShowConfirmMsg2Command}"/>
            <Button Content="Show YesNo MessageBox"
                    Command="{Binding ShowYesNoMsgCommand}"/>
            <Button Content="Show Popup Window1"
                    Command="{Binding ShowPopup1Command}"/>
            <Button Content="Show Popup Window2"
                    Command="{Binding ShowPopup2Command}"/>
        </StackPanel>
    </Grid>
</UserControl>
